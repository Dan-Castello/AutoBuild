<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="AutoBuild Automation Interface"
    MinHeight="600" MinWidth="860"
    WindowStartupLocation="CenterScreen"
    UseLayoutRounding="True"
    TextOptions.TextFormattingMode="Display"
    Background="#0F1117">
    <Window.Resources>
        <SolidColorBrush x:Key="BgPrimary"     Color="#0F1117"/>
        <SolidColorBrush x:Key="BgSecondary"   Color="#171B26"/>
        <SolidColorBrush x:Key="BgPanel"       Color="#1E2232"/>
        <SolidColorBrush x:Key="BgCard"        Color="#252B3B"/>
        <SolidColorBrush x:Key="BgHover"       Color="#2D3348"/>
        <SolidColorBrush x:Key="AccentOrange"  Color="#F5A623"/>
        <SolidColorBrush x:Key="AccentBlue"    Color="#4A9EFF"/>
        <SolidColorBrush x:Key="AccentGreen"   Color="#3EBA7E"/>
        <SolidColorBrush x:Key="AccentRed"     Color="#E85555"/>
        <SolidColorBrush x:Key="AccentYellow"  Color="#F5CE42"/>
        <SolidColorBrush x:Key="TextPrimary"   Color="#E8EAF0"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8892A8"/>
        <SolidColorBrush x:Key="TextMuted"     Color="#4A5068"/>
        <SolidColorBrush x:Key="BorderColor"   Color="#2D3348"/>

        <!-- Button Styles -->
        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background"   Value="#F5A623"/>
            <Setter Property="Foreground"   Value="#0F1117"/>
            <Setter Property="FontWeight"   Value="Bold"/>
            <Setter Property="FontSize"     Value="12"/>
            <Setter Property="Padding"      Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"       Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFB840"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#3A3F50"/>
                                <Setter Property="Foreground" Value="#5A6070"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnSecondary" TargetType="Button">
            <Setter Property="Background"   Value="#2D3348"/>
            <Setter Property="Foreground"   Value="#E8EAF0"/>
            <Setter Property="FontSize"     Value="12"/>
            <Setter Property="Padding"      Value="14,7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush"  Value="#3D4460"/>
            <Setter Property="Cursor"       Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#363D57"/>
                                <Setter Property="BorderBrush" Value="#F5A623"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#1E2232"/>
                                <Setter Property="Foreground" Value="#4A5068"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnDanger" TargetType="Button">
            <Setter Property="Background"   Value="#8B2020"/>
            <Setter Property="Foreground"   Value="#FFD0D0"/>
            <Setter Property="FontSize"     Value="12"/>
            <Setter Property="Padding"      Value="14,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"       Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B52A2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background"   Value="Transparent"/>
            <Setter Property="Foreground"   Value="#8892A8"/>
            <Setter Property="FontSize"     Value="13"/>
            <Setter Property="Padding"      Value="10,9"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"       Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}"
                                BorderThickness="3,0,0,0" BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#1E2232"/>
                                <Setter Property="Foreground" Value="#E8EAF0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputField" TargetType="TextBox">
            <Setter Property="Background"   Value="#1E2232"/>
            <Setter Property="Foreground"   Value="#E8EAF0"/>
            <Setter Property="BorderBrush"  Value="#2D3348"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"      Value="8,6"/>
            <Setter Property="FontSize"     Value="13"/>
            <Setter Property="CaretBrush"   Value="#F5A623"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#F5A623"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- FIX-01: ComboBox style with ItemContainerStyle to make dropdown text visible -->
        <Style x:Key="InputCombo" TargetType="ComboBox">
            <Setter Property="Background"   Value="#1E2232"/>
            <Setter Property="Foreground"   Value="#E8EAF0"/>
            <Setter Property="BorderBrush"  Value="#2D3348"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"      Value="8,6"/>
            <Setter Property="FontSize"     Value="13"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#1E2232"/>
                        <Setter Property="Foreground" Value="#E8EAF0"/>
                        <Setter Property="Padding"    Value="8,6"/>
                        <Setter Property="FontSize"   Value="13"/>
                        <Style.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#2D3E5C"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2D3E5C"/>
                                <Setter Property="Foreground" Value="#F5A623"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkGrid" TargetType="DataGrid">
            <Setter Property="Background"           Value="#1E2232"/>
            <Setter Property="Foreground"           Value="#E8EAF0"/>
            <Setter Property="BorderBrush"          Value="#2D3348"/>
            <Setter Property="GridLinesVisibility"  Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#252B3B"/>
            <Setter Property="RowBackground"        Value="#1E2232"/>
            <Setter Property="AlternatingRowBackground" Value="#222840"/>
            <Setter Property="HeadersVisibility"    Value="Column"/>
            <Setter Property="SelectionMode"        Value="Single"/>
            <Setter Property="AutoGenerateColumns"  Value="False"/>
            <Setter Property="CanUserAddRows"       Value="False"/>
            <Setter Property="CanUserDeleteRows"    Value="False"/>
            <Setter Property="IsReadOnly"           Value="True"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"   Value="#252B3B"/>
            <Setter Property="Foreground"   Value="#8892A8"/>
            <Setter Property="Padding"      Value="10,8"/>
            <Setter Property="FontWeight"   Value="Bold"/>
            <Setter Property="FontSize"     Value="11"/>
            <Setter Property="BorderBrush"  Value="#2D3348"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground"   Value="#E8EAF0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2D3E5C"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#252B3B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding"      Value="8,6"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#1A1E2C"/>
            <Setter Property="Width"      Value="8"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground"   Value="#8892A8"/>
            <Setter Property="FontSize"     Value="11"/>
            <Setter Property="FontWeight"   Value="Medium"/>
            <Setter Property="Margin"       Value="0,0,0,4"/>
        </Style>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground"   Value="#E8EAF0"/>
            <Setter Property="FontSize"     Value="14"/>
            <Setter Property="FontWeight"   Value="Bold"/>
            <Setter Property="Margin"       Value="0,0,0,12"/>
        </Style>
        <Style x:Key="MetricCard" TargetType="Border">
            <Setter Property="Background"     Value="#1E2232"/>
            <Setter Property="BorderBrush"    Value="#2D3348"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius"   Value="8"/>
            <Setter Property="Padding"        Value="20,16"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E8EAF0"/>
            <Setter Property="FontSize"   Value="13"/>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background"     Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Background"     Value="#1E2232"/>
            <Setter Property="Foreground"     Value="#8892A8"/>
            <Setter Property="Padding"        Value="14,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="bdr" Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,2" BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bdr" Property="BorderBrush" Value="#F5A623"/>
                                <Setter TargetName="bdr" Property="Background" Value="#252B3B"/>
                                <Setter Property="Foreground" Value="#F5A623"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="180" MaxWidth="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Column="0" Background="#171B26" BorderBrush="#2D3348" BorderThickness="0,0,1,0">
            <DockPanel>
                <Border DockPanel.Dock="Top" Padding="16,20,16,16" BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                    <StackPanel>
                        <TextBlock Text="AUTO" FontSize="22" FontWeight="Black" Foreground="#F5A623"/>
                        <TextBlock Text="BUILD" FontSize="22" FontWeight="Black" Foreground="#E8EAF0" Margin="0,-4,0,0"/>
                        <TextBlock x:Name="txtVersion" Text="Automation Interface v2.0"
                                   FontSize="10" Foreground="#4A5068" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border DockPanel.Dock="Top" Padding="16,12" BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="32"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14"
                                Background="#F5A623" VerticalAlignment="Center">
                            <TextBlock x:Name="txtUserInitial" Text="U" FontWeight="Bold"
                                       FontSize="12" Foreground="#0F1117"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="txtUserName" Text="User" FontSize="12"
                                       Foreground="#E8EAF0" FontWeight="Medium"/>
                            <TextBlock x:Name="txtUserRole" Text="Operator" FontSize="10"
                                       Foreground="#F5A623"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <StackPanel DockPanel.Dock="Top" Margin="0,8,0,0">
                    <TextBlock Text="MAIN" FontSize="10" Foreground="#4A5068"
                               Margin="16,8,0,4" FontWeight="Bold"/>
                    <Button x:Name="btnNavCatalog"     Content="  Task Catalog"      Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavExecute"     Content="  Execute Task"      Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavQueue"       Content="  Task Queue"        Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavMonitor"     Content="  Live Monitor"      Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavRunLog"      Content="  Run Log"           Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavHistory"     Content="  Execution History" Style="{StaticResource NavButton}"/>

                    <TextBlock Text="MANAGEMENT" FontSize="10" Foreground="#4A5068"
                               Margin="16,16,0,4" FontWeight="Bold"/>
                    <Button x:Name="btnNavCheckpoints" Content="  Checkpoints"       Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavArtifacts"   Content="  Artifacts"         Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavFolders"     Content="  Folder Paths"      Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavMetrics"     Content="  Metrics"           Style="{StaticResource NavButton}"/>

                    <TextBlock Text="SYSTEM" FontSize="10" Foreground="#4A5068"
                               Margin="16,16,0,4" FontWeight="Bold"/>
                    <Button x:Name="btnNavDiag"        Content="  Diagnostics"       Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavConfig"      Content="  Configuration"     Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavNewTask"     Content="  New Task"          Style="{StaticResource NavButton}"/>
                    <Button x:Name="btnNavAudit"       Content="  Audit Log"         Style="{StaticResource NavButton}"/>
                </StackPanel>

                <Border DockPanel.Dock="Bottom" Padding="16,10"
                        Background="#0F1117" BorderBrush="#2D3348" BorderThickness="0,1,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="elEngineStatus" Width="8" Height="8"
                                    Background="#3EBA7E" CornerRadius="4" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtEngineStatus" Text="Engine Ready"
                                       FontSize="11" Foreground="#8892A8" Margin="6,0,0,0"/>
                        </StackPanel>
                        <TextBlock x:Name="txtEnginePath" Text="" FontSize="10"
                                   Foreground="#4A5068" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                    </StackPanel>
                </Border>
            </DockPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Column="1">

            <!-- PROD-GUARD: Security warning banner — visible when engine runs in dev mode -->
            <Border x:Name="pnlSecurityWarning" Visibility="Collapsed"
                    Background="#7A1F1F" BorderBrush="#C0392B" BorderThickness="0,0,0,2"
                    Height="34" VerticalAlignment="Top" Panel.ZIndex="100">
                <TextBlock x:Name="txtSecurityWarning"
                           Text="⚠  SECURITY: Engine running in DEV MODE — security groups not configured. All users resolved as Operator."
                           Foreground="#FFD0D0" FontSize="12" FontWeight="SemiBold"
                           VerticalAlignment="Center" HorizontalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </Border>

            <DockPanel x:Name="pnlPageHeader" Height="48" VerticalAlignment="Top" Background="#171B26" Margin="0,0,0,0">
                <TextBlock x:Name="txtPageTitle" Text="Task Catalog"
                           FontSize="18" FontWeight="Bold" Foreground="#E8EAF0"
                           VerticalAlignment="Center" Margin="24,0,0,0" DockPanel.Dock="Left"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right"
                            Margin="0,0,16,0" VerticalAlignment="Center">
                    <Button x:Name="btnRefresh" Content="Refresh" Style="{StaticResource BtnSecondary}"
                            Padding="12,6" FontSize="11"/>
                </StackPanel>
            </DockPanel>

            <Grid x:Name="gridPages" Margin="0,48,0,0">

                <!-- PAGE: TASK CATALOG -->
                <Grid x:Name="pageCatalog" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#171B26" Padding="16,0"
                            BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBox x:Name="txtCatalogSearch" Width="220" Height="32"
                                     Style="{StaticResource InputField}" Tag="Search tasks..."/>
                            <ComboBox x:Name="cboCatalogCategory" Width="140" Height="32"
                                      Style="{StaticResource InputCombo}" Margin="8,0,0,0">
                                <ComboBoxItem Content="All Categories" IsSelected="True"/>
                                <ComboBoxItem Content="SAP"/>
                                <ComboBoxItem Content="Excel"/>
                                <ComboBoxItem Content="CSV"/>
                                <ComboBoxItem Content="Reporte"/>
                                <ComboBoxItem Content="Utilidad"/>
                            </ComboBox>
                            <TextBlock x:Name="txtCatalogCount" Text="0 tasks"
                                       FontSize="12" Foreground="#8892A8"
                                       VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </StackPanel>
                    </Border>
                    <DataGrid x:Name="gridCatalog" Grid.Row="1" Style="{StaticResource DarkGrid}"
                              Margin="16,16,16,56" BorderThickness="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Task Name"   Binding="{Binding Name}"        Width="180"/>
                            <DataGridTextColumn Header="Category"    Binding="{Binding Category}"    Width="100"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="Version"     Binding="{Binding Version}"     Width="70"/>
                            <DataGridTextColumn Header="Last Run"    Binding="{Binding LastRun}"     Width="150"/>
                            <DataGridTextColumn Header="Status"      Binding="{Binding LastStatus}"  Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border Grid.Row="1" VerticalAlignment="Bottom" Height="56"
                            Background="#171B26" BorderBrush="#2D3348" BorderThickness="0,1,0,0" Padding="16,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="btnCatalogExecute"     Content="Execute Selected"  Style="{StaticResource BtnPrimary}"/>
                            <Button x:Name="btnCatalogAddQueue"    Content="Add to Queue"      Style="{StaticResource BtnSecondary}" Margin="8,0,0,0"
                                    ToolTip="Add this task to the Task Queue without navigating away"/>
                            <Button x:Name="btnCatalogViewHistory" Content="View History"      Style="{StaticResource BtnSecondary}" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- PAGE: EXECUTE TASK -->
                <Grid x:Name="pageExecute" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="#171B26"
                            BorderBrush="#2D3348" BorderThickness="0,0,1,0" Padding="20">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="SELECT TASK" Style="{StaticResource SectionHeader}"/>
                                <TextBlock Text="Task" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="cboExecTask" Height="34" Style="{StaticResource InputCombo}"/>
                                <Border x:Name="pnlTaskInfo" Margin="0,12,0,0" Padding="12"
                                        Background="#1E2232" CornerRadius="6" Visibility="Collapsed">
                                    <StackPanel>
                                        <TextBlock x:Name="txtExecTaskDesc" Text=""
                                                   FontSize="12" Foreground="#8892A8" TextWrapping="Wrap"/>
                                        <TextBlock x:Name="txtExecTaskCat" Text=""
                                                   FontSize="11" Foreground="#F5A623" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                                <Separator Margin="0,16" Background="#2D3348"/>
                                <TextBlock Text="PARAMETERS" Style="{StaticResource SectionHeader}"/>
                                <StackPanel x:Name="pnlParams"/>
                                <Separator Margin="0,16" Background="#2D3348"/>
                                <TextBlock Text="EXECUTION MODE" Style="{StaticResource SectionHeader}"/>
                                <CheckBox x:Name="chkWhatIf"    Content="WhatIf (dry run)"         Margin="0,4"/>
                                <CheckBox x:Name="chkCheckpoint" Content="Enable Checkpoints"      Margin="0,4"/>
                                <CheckBox x:Name="chkResume"    Content="Resume from Checkpoint"   Margin="0,4"/>
                                <Separator Margin="0,16" Background="#2D3348"/>
                                <Button x:Name="btnExecuteTask" Content="EXECUTE TASK"
                                        Style="{StaticResource BtnPrimary}" Height="40" FontSize="14"/>
                                <Button x:Name="btnAddToQueue"  Content="Add to Queue"
                                        Style="{StaticResource BtnSecondary}" Margin="0,6,0,0" Height="34"
                                        ToolTip="Add this task with current parameters to the Task Queue"/>
                                <Button x:Name="btnCancelTask" Content="Cancel Running Task"
                                        Style="{StaticResource BtnDanger}" Margin="0,8,0,0" IsEnabled="False"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Padding="16,12" Background="#1E2232"
                                BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="elExecStatus" Width="10" Height="10"
                                        Background="#4A5068" CornerRadius="5" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtExecStatus" Text="Idle"
                                           FontSize="13" Foreground="#E8EAF0"
                                           Margin="8,0,0,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtExecDuration" Text=""
                                           FontSize="12" Foreground="#8892A8"
                                           Margin="16,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" Background="#0A0D14" Margin="16">
                            <ScrollViewer x:Name="svExecOutput" VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="txtExecOutput"
                                           FontFamily="Consolas" FontSize="12"
                                           Foreground="#C8D0E0" Padding="16" TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>

                <!-- PAGE: LIVE MONITOR -->
                <Grid x:Name="pageMonitor" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="300"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Padding="16,12" Background="#171B26"
                            BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ACTIVE TASKS" FontSize="12" FontWeight="Bold"
                                       Foreground="#F5A623" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtActiveCount" Text="0 running"
                                       FontSize="12" Foreground="#8892A8"
                                       Margin="12,0,0,0" VerticalAlignment="Center"/>
                            <Border Width="1" Background="#2D3348" Margin="16,4"/>
                            <TextBlock Text="Show:" FontSize="11" Foreground="#8892A8"
                                       VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <ComboBox x:Name="cboPeriodFilter" Width="110" Height="28"
                                      Style="{StaticResource InputCombo}" VerticalAlignment="Center">
                                <ComboBoxItem Content="All time"    Tag="0"   IsSelected="True"/>
                                <ComboBoxItem Content="Last 24 h"  Tag="1"/>
                                <ComboBoxItem Content="Last 7 days" Tag="7"/>
                                <ComboBoxItem Content="Last 30 days" Tag="30"/>
                            </ComboBox>
                            <Border Width="1" Background="#2D3348" Margin="16,4"/>
                            <TextBlock Text="Purge older than:" FontSize="11" Foreground="#8892A8"
                                       VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <ComboBox x:Name="cboPurgePeriod" Width="110" Height="28"
                                      Style="{StaticResource InputCombo}" VerticalAlignment="Center">
                                <ComboBoxItem Content="1 day"    Tag="1"/>
                                <ComboBoxItem Content="7 days"   Tag="7"/>
                                <ComboBoxItem Content="30 days"  Tag="30"  IsSelected="True"/>
                                <ComboBoxItem Content="90 days"  Tag="90"/>
                                <ComboBoxItem Content="180 days" Tag="180"/>
                            </ComboBox>
                            <Button x:Name="btnPurgeLogs" Content="Purge"
                                    Style="{StaticResource BtnDanger}"
                                    Padding="10,5" FontSize="11" Margin="6,0,0,0"/>
                            <Button x:Name="btnExportMonitorLog" Content="Export CSV"
                                    Style="{StaticResource BtnSecondary}"
                                    Padding="10,5" FontSize="11" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                    <DataGrid x:Name="gridMonitorJobs" Grid.Row="1"
                              Style="{StaticResource DarkGrid}" Margin="16,16,16,8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Task"     Binding="{Binding TaskName}" Width="160"/>
                            <DataGridTextColumn Header="Run ID"   Binding="{Binding RunId}"    Width="180"/>
                            <DataGridTextColumn Header="Started"  Binding="{Binding Started}"  Width="160"/>
                            <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="100"/>
                            <DataGridTextColumn Header="Status"   Binding="{Binding Status}"   Width="100"/>
                            <DataGridTextColumn Header="User"     Binding="{Binding User}"     Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border Grid.Row="2" Background="#0A0D14" Margin="16,0,16,16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="32"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="#1E2232" Padding="12,0">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="LIVE LOG STREAM" FontSize="11" FontWeight="Bold" Foreground="#F5A623"/>
                                    <Border x:Name="elLogPulse" Width="6" Height="6"
                                            Background="#3EBA7E" CornerRadius="3"
                                            Margin="8,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ScrollViewer x:Name="svLiveLog" Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="txtLiveLog"
                                           FontFamily="Consolas" FontSize="11"
                                           Foreground="#8892A8" Padding="12" TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>

                <!-- PAGE: RUN LOG (FIX-04) -->
                <Grid x:Name="pageRunLog" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Padding="16,12" Background="#171B26"
                            BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txtRunLogHeader" Text="No task running"
                                       FontSize="13" Foreground="#F5A623" VerticalAlignment="Center"
                                       FontWeight="Bold"/>
                            <Button x:Name="btnExportRunLog" Content="Export Log"
                                    Style="{StaticResource BtnSecondary}"
                                    Padding="12,6" FontSize="11" Margin="24,0,0,0"/>
                            <Button x:Name="btnClearRunLog" Content="Clear"
                                    Style="{StaticResource BtnSecondary}"
                                    Padding="10,6" FontSize="11" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="1" Background="#0A0D14" Margin="16">
                        <ScrollViewer x:Name="svRunLog" VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="txtRunLog"
                                       FontFamily="Consolas" FontSize="12"
                                       Foreground="#C8D0E0" Padding="16" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <!-- PAGE: EXECUTION HISTORY -->
                <Grid x:Name="pageHistory" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="250"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#171B26" Padding="16,0"
                            BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBox x:Name="txtHistorySearch" Width="200" Height="32"
                                     Style="{StaticResource InputField}" Tag="Filter by task..."/>
                            <ComboBox x:Name="cboHistoryStatus" Width="120" Height="32"
                                      Style="{StaticResource InputCombo}" Margin="8,0,0,0">
                                <ComboBoxItem Content="All Status" IsSelected="True"/>
                                <ComboBoxItem Content="OK"/>
                                <ComboBoxItem Content="ERROR"/>
                                <ComboBoxItem Content="RUNNING"/>
                            </ComboBox>
                            <Button x:Name="btnHistoryFilter"  Content="Apply Filter"
                                    Style="{StaticResource BtnSecondary}" Margin="8,0,0,0" Height="32"/>
                            <Button x:Name="btnExportHistory"  Content="Export CSV"
                                    Style="{StaticResource BtnSecondary}" Margin="8,0,0,0" Height="32"/>
                        </StackPanel>
                    </Border>
                    <DataGrid x:Name="gridHistory" Grid.Row="1"
                              Style="{StaticResource DarkGrid}" Margin="16,16,16,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Run ID"   Binding="{Binding RunId}"   Width="180"/>
                            <DataGridTextColumn Header="Task"     Binding="{Binding Task}"    Width="150"/>
                            <DataGridTextColumn Header="Started"  Binding="{Binding Started}" Width="160"/>
                            <DataGridTextColumn Header="Elapsed"  Binding="{Binding Elapsed}" Width="90"/>
                            <DataGridTextColumn Header="Status"   Binding="{Binding Status}"  Width="80"/>
                            <DataGridTextColumn Header="Entries"  Binding="{Binding Entries}" Width="70"/>
                            <DataGridTextColumn Header="User"     Binding="{Binding User}"    Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border Grid.Row="2" Background="#0A0D14" Margin="16,8,16,16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="32"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="#1E2232" Padding="12,0">
                                <TextBlock x:Name="txtHistoryDetailTitle" Text="SELECT A RUN TO VIEW LOGS"
                                           FontSize="11" FontWeight="Bold" Foreground="#8892A8"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <ScrollViewer x:Name="svHistoryDetail" Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="txtHistoryDetail"
                                           FontFamily="Consolas" FontSize="11"
                                           Foreground="#8892A8" Padding="12" TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>

                <!-- PAGE: CHECKPOINTS -->
                <Grid x:Name="pageCheckpoints" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>
                    <DataGrid x:Name="gridCheckpoints" Grid.Row="0"
                              Style="{StaticResource DarkGrid}" Margin="16,16,16,8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Checkpoint File" Binding="{Binding Name}"     Width="*"/>
                            <DataGridTextColumn Header="Task"            Binding="{Binding TaskName}" Width="150"/>
                            <DataGridTextColumn Header="Modified"        Binding="{Binding Modified}" Width="160"/>
                            <DataGridTextColumn Header="Size (KB)"       Binding="{Binding SizeKB}"   Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border Grid.Row="0" VerticalAlignment="Bottom" Height="56"
                            Background="#171B26" BorderBrush="#2D3348" BorderThickness="0,1,0,0" Padding="16,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="btnResumeCheckpoint" Content="Resume from Checkpoint"
                                    Style="{StaticResource BtnPrimary}"/>
                            <Button x:Name="btnViewCheckpoint"   Content="View Contents"
                                    Style="{StaticResource BtnSecondary}" Margin="8,0,0,0"/>
                            <Button x:Name="btnDeleteCheckpoint" Content="Delete"
                                    Style="{StaticResource BtnDanger}"   Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="1" Background="#0A0D14" Margin="16,0,16,16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="#1E2232" Padding="12,0">
                                <TextBlock x:Name="txtCheckpointTitle" Text="SELECT A CHECKPOINT TO VIEW"
                                           FontSize="11" FontWeight="Bold" Foreground="#8892A8"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <ScrollViewer x:Name="svCheckpointDetail" Grid.Row="1"
                                          VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="txtCheckpointDetail"
                                           FontFamily="Consolas" FontSize="11"
                                           Foreground="#8892A8" Padding="12" TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>

                <!-- PAGE: ARTIFACTS -->
                <Grid x:Name="pageArtifacts" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#171B26" Padding="16,0"
                            BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBox x:Name="txtArtifactSearch" Width="200" Height="32"
                                     Style="{StaticResource InputField}" Tag="Filter artifacts..."/>
                            <Button x:Name="btnArtifactFilter"  Content="Filter"
                                    Style="{StaticResource BtnSecondary}" Margin="8,0,0,0" Height="32"/>
                            <TextBlock x:Name="txtArtifactCount" Text=""
                                       FontSize="12" Foreground="#8892A8"
                                       VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </StackPanel>
                    </Border>
                    <DataGrid x:Name="gridArtifacts" Grid.Row="1"
                              Style="{StaticResource DarkGrid}" Margin="16,16,16,60">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="File"      Binding="{Binding Name}"      Width="*"/>
                            <DataGridTextColumn Header="Type"      Binding="{Binding Extension}" Width="70"/>
                            <DataGridTextColumn Header="Directory" Binding="{Binding Directory}" Width="90"/>
                            <DataGridTextColumn Header="Size (KB)" Binding="{Binding SizeKB}"    Width="80"/>
                            <DataGridTextColumn Header="Modified"  Binding="{Binding Modified}"  Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border Grid.Row="1" VerticalAlignment="Bottom" Height="56"
                            Background="#171B26" BorderBrush="#2D3348" BorderThickness="0,1,0,0" Padding="16,0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="btnOpenArtifact"    Content="Open / Preview"  Style="{StaticResource BtnPrimary}"/>
                            <Button x:Name="btnDownloadArtifact" Content="Save As..."      Style="{StaticResource BtnSecondary}" Margin="8,0,0,0"/>
                            <Button x:Name="btnDeleteArtifact"  Content="Delete"          Style="{StaticResource BtnDanger}"    Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- PAGE: FOLDER PATHS (FIX-02) -->
                <Grid x:Name="pageFolders" Visibility="Collapsed">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="28">
                            <TextBlock Text="FOLDER PATH CONFIGURATION" Style="{StaticResource SectionHeader}"/>
                            <TextBlock Text="Drag and drop folders from Explorer onto each field, or type the path manually."
                                       FontSize="12" Foreground="#8892A8" TextWrapping="Wrap" Margin="0,0,0,20"/>

                            <TextBlock Text="Input Folder" Style="{StaticResource FieldLabel}"/>
                            <TextBlock Text="Drop source data files here (read by tasks)" FontSize="10" Foreground="#4A5068" Margin="0,0,0,4"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="txtInputDir" Grid.Column="0" Height="34" Style="{StaticResource InputField}"
                                         AllowDrop="True" ToolTip="Drop a folder here or type a path"/>
                                <Button x:Name="btnBrowseInput" Grid.Column="1" Content="Browse..."
                                        Style="{StaticResource BtnSecondary}" Margin="6,0,0,0" Height="34" Padding="10,0"/>
                            </Grid>

                            <TextBlock Text="Output Folder" Style="{StaticResource FieldLabel}" Margin="0,16,0,4"/>
                            <TextBlock Text="Drop destination folder for generated files" FontSize="10" Foreground="#4A5068" Margin="0,0,0,4"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="txtOutputDir" Grid.Column="0" Height="34" Style="{StaticResource InputField}"
                                         AllowDrop="True" ToolTip="Drop a folder here or type a path"/>
                                <Button x:Name="btnBrowseOutput" Grid.Column="1" Content="Browse..."
                                        Style="{StaticResource BtnSecondary}" Margin="6,0,0,0" Height="34" Padding="10,0"/>
                            </Grid>

                            <TextBlock Text="Reports Folder" Style="{StaticResource FieldLabel}" Margin="0,16,0,4"/>
                            <TextBlock Text="Drop destination folder for report/diagnostic files" FontSize="10" Foreground="#4A5068" Margin="0,0,0,4"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="txtReportsDir" Grid.Column="0" Height="34" Style="{StaticResource InputField}"
                                         AllowDrop="True" ToolTip="Drop a folder here or type a path"/>
                                <Button x:Name="btnBrowseReports" Grid.Column="1" Content="Browse..."
                                        Style="{StaticResource BtnSecondary}" Margin="6,0,0,0" Height="34" Padding="10,0"/>
                            </Grid>

                            <Separator Margin="0,20" Background="#2D3348"/>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="btnApplyFolders" Content="Apply &amp; Open Folders"
                                        Style="{StaticResource BtnPrimary}"/>
                                <Button x:Name="btnResetFolders" Content="Reset to Defaults"
                                        Style="{StaticResource BtnSecondary}" Margin="8,0,0,0"/>
                            </StackPanel>
                            <TextBlock x:Name="txtFolderStatus" Text="" FontSize="12"
                                       Foreground="#3EBA7E" Margin="0,10,0,0"/>

                            <Border Margin="0,20,0,0" Padding="14" Background="#1E2232" CornerRadius="6">
                                <TextBlock FontSize="11" Foreground="#8892A8" TextWrapping="Wrap"
                                           Text="Tip: You can drag a folder from Windows Explorer directly onto any field above. Paths are applied immediately to the current session. To persist them across restarts, save them via Configuration."/>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <!-- PAGE: METRICS -->
                <ScrollViewer x:Name="pageMetrics" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="24">
                        <TextBlock Text="OVERVIEW" FontSize="12" FontWeight="Bold" Foreground="#4A5068" Margin="0,0,0,12"/>
                        <UniformGrid Columns="4" Rows="1">
                            <Border Style="{StaticResource MetricCard}" Margin="0,0,8,0">
                                <StackPanel>
                                    <TextBlock Text="TOTAL RUNS" FontSize="10" FontWeight="Bold" Foreground="#8892A8"/>
                                    <TextBlock x:Name="txtMetricTotal" Text="0" FontSize="36" FontWeight="Black" Foreground="#E8EAF0"/>
                                    <TextBlock Text="all time" FontSize="11" Foreground="#4A5068"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource MetricCard}" Margin="4,0,4,0">
                                <StackPanel>
                                    <TextBlock Text="SUCCESS RATE" FontSize="10" FontWeight="Bold" Foreground="#8892A8"/>
                                    <TextBlock x:Name="txtMetricSuccess" Text="0%" FontSize="36" FontWeight="Black" Foreground="#3EBA7E"/>
                                    <TextBlock x:Name="txtMetricOKErr" Text="0 OK / 0 Error" FontSize="11" Foreground="#4A5068"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource MetricCard}" Margin="4,0,4,0">
                                <StackPanel>
                                    <TextBlock Text="AVG DURATION" FontSize="10" FontWeight="Bold" Foreground="#8892A8"/>
                                    <TextBlock x:Name="txtMetricAvg" Text="0s" FontSize="36" FontWeight="Black" Foreground="#F5A623"/>
                                    <TextBlock Text="per run" FontSize="11" Foreground="#4A5068"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource MetricCard}" Margin="8,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="TOP TASK" FontSize="10" FontWeight="Bold" Foreground="#8892A8"/>
                                    <TextBlock x:Name="txtMetricTop" Text="N/A" FontSize="20" FontWeight="Black" Foreground="#4A9EFF" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="most executed" FontSize="11" Foreground="#4A5068"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                        <TextBlock Text="TASK FREQUENCY" FontSize="12" FontWeight="Bold" Foreground="#4A5068" Margin="0,28,0,12"/>
                        <Border Background="#1E2232" CornerRadius="8" Padding="16">
                            <DataGrid x:Name="gridMetricTasks" Style="{StaticResource DarkGrid}" MaxHeight="300" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Task"  Binding="{Binding Name}"  Width="200"/>
                                    <DataGridTextColumn Header="Runs"  Binding="{Binding Count}" Width="80"/>
                                    <DataGridTextColumn Header="Bar"   Binding="{Binding Bar}"   Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                        <TextBlock Text="RECENT ERRORS" FontSize="12" FontWeight="Bold" Foreground="#4A5068" Margin="0,28,0,12"/>
                        <Border Background="#1E2232" CornerRadius="8" Padding="16">
                            <DataGrid x:Name="gridMetricErrors" Style="{StaticResource DarkGrid}" MaxHeight="240" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Timestamp" Binding="{Binding ts}"      Width="160"/>
                                    <DataGridTextColumn Header="Task"      Binding="{Binding task}"    Width="140"/>
                                    <DataGridTextColumn Header="Message"   Binding="{Binding message}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- PAGE: DIAGNOSTICS (FIX-03) -->
                <Grid x:Name="pageDiag" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Padding="16,12" Background="#171B26"
                            BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="btnRunDiag" Content="Run Diagnostics" Style="{StaticResource BtnPrimary}"/>
                            <TextBlock x:Name="txtDiagSummary" Text="Click Run Diagnostics to check environment"
                                       FontSize="12" Foreground="#8892A8" Margin="16,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <DataGrid x:Name="gridDiag" Grid.Row="1" Style="{StaticResource DarkGrid}" Margin="16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="110"/>
                            <DataGridTextColumn Header="Item"     Binding="{Binding Item}"     Width="180"/>
                            <DataGridTextColumn Header="Value"    Binding="{Binding Value}"    Width="120"/>
                            <DataGridTextColumn Header="Status"   Binding="{Binding Status}"   Width="70"/>
                            <DataGridTextColumn Header="Message"  Binding="{Binding Message}"  Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- PAGE: CONFIGURATION -->
                <Grid x:Name="pageConfig" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Padding="16,12" Background="#171B26"
                                BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="btnSaveConfig"   Content="Save Configuration" Style="{StaticResource BtnPrimary}"/>
                                <Button x:Name="btnReloadConfig" Content="Reload"              Style="{StaticResource BtnSecondary}" Margin="8,0,0,0"/>
                                <TextBlock x:Name="txtConfigStatus" Text="" FontSize="12"
                                           Foreground="#8892A8" Margin="16,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <TextBox x:Name="txtConfigEditor" Grid.Row="1"
                                 Style="{StaticResource InputField}" Margin="16"
                                 AcceptsReturn="True" TextWrapping="NoWrap"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="13"
                                 Background="#0A0D14" BorderThickness="0"/>
                    </Grid>
                    <Border Grid.Column="1" Background="#171B26"
                            BorderBrush="#2D3348" BorderThickness="1,0,0,0" Padding="20">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="CONFIGURATION GUIDE" Style="{StaticResource SectionHeader}"/>
                                <TextBlock Text="engine" FontSize="13" FontWeight="Bold" Foreground="#F5A623" Margin="0,8,0,4"/>
                                <TextBlock FontSize="12" Foreground="#8892A8" TextWrapping="Wrap"
                                           Text="logLevel: DEBUG|INFO|WARN|ERROR&#10;maxRetries: 0-10&#10;retryDelaySeconds: seconds between retries"/>
                                <TextBlock Text="sap" FontSize="13" FontWeight="Bold" Foreground="#F5A623" Margin="0,16,0,4"/>
                                <TextBlock FontSize="12" Foreground="#8892A8" TextWrapping="Wrap"
                                           Text="systemId: SAP System ID (e.g. PRD)&#10;client: SAP Client number&#10;language: Login language (ES, EN)&#10;timeout: Session timeout in seconds"/>
                                <TextBlock Text="excel" FontSize="13" FontWeight="Bold" Foreground="#F5A623" Margin="0,16,0,4"/>
                                <TextBlock FontSize="12" Foreground="#8892A8" TextWrapping="Wrap"
                                           Text="visible: Show Excel window (false=headless)&#10;screenUpdating: Enable screen refresh"/>
                                <TextBlock Text="reports" FontSize="13" FontWeight="Bold" Foreground="#F5A623" Margin="0,16,0,4"/>
                                <TextBlock FontSize="12" Foreground="#8892A8" TextWrapping="Wrap"
                                           Text="defaultFormat: Output format (xlsx, csv)&#10;retentionDays: Days to keep artifacts (1-365)"/>
                                <Border Margin="0,20,0,0" Padding="12" Background="#1E2232" CornerRadius="6">
                                    <TextBlock FontSize="11" Foreground="#E85555" TextWrapping="Wrap"
                                               Text="Changes are validated before saving. Invalid JSON or out-of-range values will be rejected. All config changes are audit-logged."/>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <!-- PAGE: NEW TASK -->
                <Grid x:Name="pageNewTask" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="#171B26"
                            BorderBrush="#2D3348" BorderThickness="0,0,1,0" Padding="24">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="CREATE NEW TASK" Style="{StaticResource SectionHeader}"/>
                                <TextBlock Text="Task Name" Style="{StaticResource FieldLabel}"/>
                                <TextBlock Text="Lowercase letters, numbers, and underscores only"
                                           FontSize="10" Foreground="#4A5068" Margin="0,0,0,4"/>
                                <TextBox x:Name="txtNewTaskName" Height="34" Style="{StaticResource InputField}"/>
                                <TextBlock x:Name="txtNewTaskNameError" Text="Invalid name: use letters, numbers, _ or - only"
                                           FontSize="11" Foreground="#E85555" Margin="0,2,0,0" Visibility="Collapsed"/>
                                <TextBlock Text="Category" Style="{StaticResource FieldLabel}" Margin="0,12,0,4"/>
                                <ComboBox x:Name="cboNewTaskCategory" Height="34" Style="{StaticResource InputCombo}">
                                    <ComboBoxItem Content="SAP"/>
                                    <ComboBoxItem Content="Excel" IsSelected="True"/>
                                    <ComboBoxItem Content="CSV"/>
                                    <ComboBoxItem Content="Reporte"/>
                                    <ComboBoxItem Content="Utilidad"/>
                                </ComboBox>
                                <TextBlock Text="Description" Style="{StaticResource FieldLabel}" Margin="0,12,0,4"/>
                                <TextBox x:Name="txtNewTaskDesc" Height="64"
                                         Style="{StaticResource InputField}" AcceptsReturn="True" TextWrapping="Wrap"/>
                                <Separator Margin="0,16" Background="#2D3348"/>
                                <Button x:Name="btnCreateTask" Content="CREATE TASK FILE"
                                        Style="{StaticResource BtnPrimary}" Height="40" FontSize="14"/>
                                <TextBlock x:Name="txtCreateTaskResult" Text="" FontSize="12"
                                           Foreground="#3EBA7E" TextWrapping="Wrap"
                                           Margin="0,12,0,0" Visibility="Collapsed"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="#1E2232" Padding="16,0">
                            <TextBlock Text="TASK FILE PREVIEW" FontSize="11" FontWeight="Bold"
                                       Foreground="#8892A8" VerticalAlignment="Center"/>
                        </Border>
                        <TextBox x:Name="txtNewTaskPreview" Grid.Row="1"
                                 Style="{StaticResource InputField}" IsReadOnly="True" AcceptsReturn="True"
                                 FontFamily="Consolas" FontSize="12"
                                 Background="#0A0D14" BorderThickness="0" VerticalScrollBarVisibility="Auto"/>
                    </Grid>
                </Grid>

                <!-- PAGE: TASK QUEUE -->
                <Grid x:Name="pageQueue" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="180"/>
                    </Grid.RowDefinitions>

                    <!-- QUEUE TOOLBAR -->
                    <Border Grid.Row="0" Background="#171B26" BorderBrush="#2D3348"
                            BorderThickness="0,0,0,1" Padding="10,8">
                        <WrapPanel Orientation="Horizontal">
                            <!-- Add task -->
                            <ComboBox x:Name="cboQueueAddTask" Width="175" Height="30"
                                      Style="{StaticResource InputCombo}"
                                      Margin="0,2,4,2"
                                      ToolTip="Select task to add to queue"/>
                            <Button x:Name="btnQueueAdd"
                                    Content="+ Add" Style="{StaticResource BtnPrimary}"
                                    Height="30" Padding="10,0" Margin="0,2,12,2"/>
                            <!-- Run controls -->
                            <Button x:Name="btnQueueRunAll"
                                    Content="Run All" Style="{StaticResource BtnPrimary}"
                                    Height="30" Padding="10,0" Margin="0,2,4,2"/>
                            <Button x:Name="btnQueueRunSelected"
                                    Content="Run Sel." Style="{StaticResource BtnSecondary}"
                                    Height="30" Padding="10,0" Margin="0,2,4,2"/>
                            <Button x:Name="btnQueueRunNext"
                                    Content="> Next" Style="{StaticResource BtnSecondary}"
                                    Height="30" Padding="10,0" Margin="0,2,4,2"/>
                            <Button x:Name="btnQueuePause"
                                    Content="| Pause" Style="{StaticResource BtnSecondary}"
                                    Height="30" Padding="10,0" Margin="0,2,4,2"/>
                            <Button x:Name="btnQueueResume"
                                    Content="Resume" Style="{StaticResource BtnSecondary}"
                                    Height="30" Padding="10,0" Margin="0,2,4,2"/>
                            <Button x:Name="btnQueueStop"
                                    Content="Stop" Style="{StaticResource BtnDanger}"
                                    Height="30" Padding="10,0" Margin="0,2,12,2"/>
                            <!-- Status -->
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,2,12,2">
                                <Border x:Name="elQueueStatus" Width="10" Height="10"
                                        Background="#4A5068" CornerRadius="5" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtQueueStatus" Text="Stopped"
                                           FontSize="12" Foreground="#8892A8"
                                           Margin="6,0,0,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtQueueActiveTask" Text=""
                                           FontSize="11" Foreground="#F5A623"
                                           Margin="10,0,0,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtQueueElapsed" Text=""
                                           FontSize="11" Foreground="#8892A8"
                                           Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <!-- Queue file ops -->
                            <Button x:Name="btnQueueSave"
                                    Content="Save" Style="{StaticResource BtnSecondary}"
                                    Height="30" Padding="10,0" Margin="0,2,4,2"
                                    ToolTip="Save queue to JSON"/>
                            <Button x:Name="btnQueueLoad"
                                    Content="Load" Style="{StaticResource BtnSecondary}"
                                    Height="30" Padding="10,0" Margin="0,2,4,2"
                                    ToolTip="Load queue from JSON"/>
                            <Button x:Name="btnQueueClear"
                                    Content="Clear" Style="{StaticResource BtnDanger}"
                                    Height="30" Padding="10,0" Margin="0,2,0,2"
                                    ToolTip="Remove all non-running tasks"/>
                        </WrapPanel>
                    </Border>

                    <!-- QUEUE DATAGRID - fills all available space -->
                    <DataGrid x:Name="gridQueue" Grid.Row="1"
                              Style="{StaticResource DarkGrid}"
                              SelectionMode="Extended"
                              AllowDrop="True"
                              CanUserReorderColumns="False"
                              CanUserSortColumns="False"
                              Margin="12,8,12,4">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#"          Binding="{Binding Order}"       Width="42"/>
                            <DataGridTextColumn Header="Task"       Binding="{Binding Name}"        Width="170"/>
                            <DataGridTextColumn Header="Status"     Binding="{Binding Status}"      Width="90"/>
                            <DataGridTextColumn Header="Pri."       Binding="{Binding Priority}"    Width="44"/>
                            <DataGridTextColumn Header="Parameters" Binding="{Binding Parameters}"  Width="*"/>
                            <DataGridTextColumn Header="Started"    Binding="{Binding StartedAt}"   Width="140"/>
                            <DataGridTextColumn Header="Ended"      Binding="{Binding CompletedAt}" Width="140"/>
                            <DataGridTextColumn Header="Retries"    Binding="{Binding RetryCount}"  Width="58"/>
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem x:Name="ctxQueueEdit"      Header="Edit parameters..."/>
                                <MenuItem x:Name="ctxQueueRunSel"    Header="Run selected"/>
                                <MenuItem x:Name="ctxQueueSkip"      Header="Skip task"/>
                                <MenuItem x:Name="ctxQueueCancel"    Header="Cancel active task"/>
                                <Separator/>
                                <MenuItem x:Name="ctxQueueMoveUp"    Header="Move up"/>
                                <MenuItem x:Name="ctxQueueMoveDown"  Header="Move down"/>
                                <MenuItem x:Name="ctxQueueMoveFirst" Header="Move to top"/>
                                <MenuItem x:Name="ctxQueueMoveLast"  Header="Move to bottom"/>
                                <Separator/>
                                <MenuItem x:Name="ctxQueueRemove"    Header="Remove from queue"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>

                    <!-- OUTPUT PANEL -->
                    <Border Grid.Row="2" Background="#0A0D14" Margin="12,0,12,12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="28"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="#1E2232" Padding="12,0">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="ACTIVE OUTPUT"
                                               FontSize="10" FontWeight="Bold" Foreground="#F5A623"/>
                                    <TextBlock x:Name="txtQueueOutputTitle" Text=""
                                               FontSize="10" Foreground="#8892A8" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Border>
                            <ScrollViewer x:Name="svQueueOutput" Grid.Row="1"
                                          VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="txtQueueOutput"
                                           FontFamily="Consolas" FontSize="11"
                                           Foreground="#C8D0E0" Padding="10"
                                           TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>

                <!-- PAGE: AUDIT LOG -->
                <Grid x:Name="pageAudit" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#171B26" Padding="16,0"
                            BorderBrush="#2D3348" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="btnExportAudit" Content="Export Audit CSV" Style="{StaticResource BtnSecondary}"/>
                            <TextBlock Text="All user actions are recorded here."
                                       FontSize="12" Foreground="#8892A8" Margin="16,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <DataGrid x:Name="gridAudit" Grid.Row="1" Style="{StaticResource DarkGrid}" Margin="16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Timestamp" Binding="{Binding ts}"     Width="160"/>
                            <DataGridTextColumn Header="User"      Binding="{Binding user}"   Width="100"/>
                            <DataGridTextColumn Header="Role"      Binding="{Binding role}"   Width="90"/>
                            <DataGridTextColumn Header="Action"    Binding="{Binding action}" Width="150"/>
                            <DataGridTextColumn Header="Target"    Binding="{Binding target}" Width="160"/>
                            <DataGridTextColumn Header="Detail"    Binding="{Binding detail}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

            </Grid><!-- end pages -->
        </Grid><!-- end main content -->
    </Grid>
</Window>
